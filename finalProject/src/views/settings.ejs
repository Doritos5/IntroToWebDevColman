<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
    <meta charset="UTF-8">
    <title>Settings - Profile Management</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/settings/settings.css">
</head>
<body>

<div class="settings-container">
    <h2>הוספת פרופיל חדש</h2>

    <form id="add-profile-form">
        <div class="mb-3">
            <label for="profileName" class="form-label visually-hidden">שם הפרופיל</label>
            <input type="text" class="form-control" id="profileName" placeholder="שם הפרופיל" required>
        </div>
        <button type="submit" class="btn btn-danger">הוסף פרופיל</button>
    </form>

    <hr class="my-4"> <h2>ניהול פרופילים קיימים</h2>
    <ul id="profiles-list" class="list-group">

        <% if (profiles && profiles.length > 0) { %>
            <% profiles.forEach(profile => { %>
                <li class="list-group-item d-flex justify-content-between align-items-center">

                    <span><%= profile.displayName %></span>

                    <button
                            class="btn btn-outline-danger btn-sm btn-delete-profile"
                            data-profile-id="<%= profile.id %>"
                    >
                        מחק
                    </button>
                </li>
            <% }) %>
        <% } else { %>
            <li class="list-group-item">אין פרופילים להצגה.</li>
        <% } %>
    </ul>

    <div id="message-container" class="mt-3"></div>
    <a href="/profiles_page" class="btn btn-secondary mt-3">חזרה לבחירת פרופיל</a>
</div>

<script src="/settings/settings.js"></script>

</body>
</html>