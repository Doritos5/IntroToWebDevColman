<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>DORGUYFLIX - Register</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="../login/styles.css" rel="stylesheet" />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/dompurify/2.2.9/purify.min.js"></script>
    <script defer src="register.js"></script>
</head>

<body class="login-page">
<div class="bg-hero"></div>

<div class="brand">DORGUYFLIX</div>

<main class="page-wrap">
    <section class="login-card">
        <h1 class="mb-4 fw-bold">Create your account</h1>

        <form id="register-form" novalidate>
            <div class="mb-3">
                <label for="register-email" class="form-label">Email</label>
                <input type="email" class="form-control" id="register-email" placeholder="name@example.com" />
                <small id="registerEmailError" class="text-danger d-none">Please enter a valid email address.</small>
            </div>
            <div class="mb-3">
                <label for="register-username" class="form-label">Username</label>
                <input type="text" class="form-control" id="register-username" placeholder="Username" />
                <small id="registerUsernameError" class="text-danger d-none">Username must be at least 3 characters and contain only letters, numbers, spaces or underscores.</small>
            </div>
            <div class="mb-2">
                <label for="register-password" class="form-label">Password</label>
                <input type="password" class="form-control" id="register-password" placeholder="Password" />
                <small id="registerPasswordError" class="text-danger d-none">Password must be at least 3 characters.</small>
            </div>

            <div class="d-grid gap-2 mt-4">
                <button type="submit" class="btn login-btn btn-lg">Register</button>
                <div class="text-center fw-bold text-white">
                    <a href="/" class="link-muted tiny">Already have an account? Sign in</a>
                </div>
            </div>

            <div id="registerServerError" class="alert alert-danger d-none mt-3" role="alert"></div>
            <div id="registerSuccess" class="alert alert-success d-none mt-3" role="alert"></div>
        </form>
    </section>
</main>

<footer class="container d-flex justify-content-center tiny mb-4">
    We would like to get 100 please.
</footer>

</body>
</html>